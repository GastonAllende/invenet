{
  "name": "trade-ui",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/trade/ui/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:trade", "type:ui"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/trade/ui"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
