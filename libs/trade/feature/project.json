{
  "name": "trade-feature",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/trade/feature/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:trade", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/trade/feature"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
