{
  "name": "invenet-api",
  "projectType": "application",
  "sourceRoot": "apps/Invenet.Api",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet build apps/Invenet.Api/Invenet.Api.csproj"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet test apps/Invenet.Api/Invenet.Api.csproj"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet watch --project apps/Invenet.Api/Invenet.Api.csproj run --launch-profile https"
      }
    }
  }
}
